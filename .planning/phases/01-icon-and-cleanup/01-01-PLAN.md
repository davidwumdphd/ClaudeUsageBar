---
phase: 01-icon-and-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/ClaudeUsageBar.swift]
autonomous: false

must_haves:
  truths:
    - "Menu bar icon is monochrome and adapts to light/dark mode automatically"
    - "Coffee/donation button is gone from the popover"
    - "Popover content is not horizontally clipped"
    - "Icon renders crisply on Retina (SF Symbol vector, not bitmap)"
  artifacts:
    - path: "app/ClaudeUsageBar.swift"
      provides: "Updated status icon, cleaned popover"
      contains: "NSImage(systemSymbolName"
  key_links:
    - from: "updateStatusIcon"
      to: "NSImage(systemSymbolName: \"sparkle\")"
      via: "SF Symbol API"
      pattern: "systemSymbolName.*sparkle"
    - from: "updateStatusIcon"
      to: "isTemplate = true"
      via: "Template image property"
      pattern: "isTemplate\\s*=\\s*true"
    - from: "popover.contentSize"
      to: ".frame(width: 360)"
      via: "Matching width values"
      pattern: "width:\\s*360"
---

<objective>
Replace the custom-drawn colored star icon with an SF Symbol "sparkle" template image, remove the donation button, and fix the popover width mismatch. All changes are in the single source file.

Purpose: Make the app look like a native macOS menu bar app -- monochrome icon, no branding clutter, no clipped content.
Output: Updated `app/ClaudeUsageBar.swift` that builds cleanly and produces a polished menu bar app.
</objective>

<execution_context>
@/Users/dwu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dwu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-icon-and-cleanup/01-CONTEXT.md
@.planning/phases/01-icon-and-cleanup/01-RESEARCH.md
@app/ClaudeUsageBar.swift
@app/build.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace colored star icon with SF Symbol sparkle template</name>
  <files>app/ClaudeUsageBar.swift</files>
  <action>
  Rewrite `updateStatusIcon(percentage:)` (lines 216-235) to use an SF Symbol instead of the custom-drawn colored star. The new implementation:

  1. Replace the entire function body with:
     - Create `NSImage(systemSymbolName: "sparkle", accessibilityDescription: "Claude usage")`
     - Set `icon.isTemplate = true` (CRITICAL -- without this, the icon won't adapt to dark/light mode)
     - Assign to `button.image`
     - Keep `button.title = " \(percentage)%"` unchanged

  2. Delete the entire `createSparkIcon(color:)` function (lines 237-270). This is the 33-line NSBezierPath custom drawing function that is no longer needed.

  3. Remove the color-determination block (lines 219-227) -- the green/yellow/red NSColor logic. The icon is now always the same monochrome sparkle regardless of usage level. Do NOT leave dead code.

  The resulting `updateStatusIcon` should be approximately:
  ```swift
  func updateStatusIcon(percentage: Int) {
      guard let button = statusItem.button else { return }
      if let icon = NSImage(systemSymbolName: "sparkle", accessibilityDescription: "Claude usage") {
          icon.isTemplate = true
          button.image = icon
      }
      button.title = " \(percentage)%"
  }
  ```

  Note: The `updateStatusIcon` signature stays as `(percentage: Int)` -- Phase 2 will extend it later for pacing. Do not change the signature or call sites.
  </action>
  <verify>
  1. Grep for `createSparkIcon` -- should return zero results (function fully removed)
  2. Grep for `isTemplate = false` -- should return zero results (old pattern gone)
  3. Grep for `NSBezierPath` -- should return zero results in this file (custom drawing gone)
  4. Grep for `systemSymbolName.*sparkle` -- should return one result (new icon)
  5. Grep for `isTemplate = true` -- should return one result
  </verify>
  <done>
  `updateStatusIcon` uses SF Symbol "sparkle" with isTemplate=true. `createSparkIcon` function is deleted. No color-determination dead code remains. The function signature is unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove donation button and fix popover width</name>
  <files>app/ClaudeUsageBar.swift</files>
  <action>
  Two changes in the same file:

  1. **Remove the coffee/donation button** (lines 982-993): Delete the entire "Support Section" block:
     ```swift
     // Support Section
     Button(action: {
         NSWorkspace.shared.open(URL(string: "https://donate.stripe.com/...")!)
     }) {
         HStack(spacing: 4) {
             Text("...")
             Text("Buy Dev a Coffee")
         }
     }
     .buttonStyle(.borderless)
     .font(.caption)
     .foregroundColor(.orange)
     ```
     Let the layout collapse naturally -- no replacement element. The Settings button below moves up.

  2. **Fix popover width** (line 38): Change `popover.contentSize = NSSize(width: 320, height: 260)` to `popover.contentSize = NSSize(width: 360, height: 260)`. The SwiftUI view already uses `.frame(width: 360)` at line 1091, so this reconciles the mismatch that causes 40px of horizontal clipping.

  Do NOT change the `.frame(width: 360)` in the SwiftUI view -- it's already correct. Only change the popover contentSize.
  </action>
  <verify>
  1. Grep for `donate.stripe.com` -- should return zero results
  2. Grep for `Buy Dev a Coffee` -- should return zero results
  3. Grep for `width: 320` -- should return zero results (old width gone)
  4. Grep for `contentSize.*width: 360` -- should return one result (new width)
  5. Build the app: `cd /Users/dwu/Code/ClaudeUsageBar/app && bash build.sh` -- should compile without errors
  </verify>
  <done>
  Donation button is removed. Popover contentSize width matches SwiftUI frame width at 360. App builds successfully with `build.sh`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  Replaced the colored star icon with a monochrome SF Symbol "sparkle" template image, removed the donation button from the popover, and fixed the popover width mismatch. The app was built and launched via build.sh.
  </what-built>
  <how-to-verify>
  The app should have launched automatically after build. Check:
  1. **Menu bar icon**: Look for a monochrome sparkle icon in the menu bar. It should be a simple outline that matches the style of other system menu bar icons. If your menu bar is dark (dark mode), the icon should be light. If light mode, the icon should be dark.
  2. **Percentage text**: The usage percentage should still display next to the icon (e.g., "75%").
  3. **Click the icon** to open the popover:
     - The "Buy Dev a Coffee" button should be gone
     - Content should not be clipped on the right edge (the width fix)
     - No extra blank space where the button used to be (layout should collapse naturally)
  4. **Icon size**: The sparkle icon should be appropriately sized relative to other menu bar icons -- not too large, not too small.
  </how-to-verify>
  <resume-signal>Type "approved" if everything looks good, or describe any issues (e.g., "icon too large", "still see clipping", etc.)</resume-signal>
</task>

</tasks>

<verification>
- App compiles via `bash app/build.sh` without errors
- No references to `createSparkIcon`, `isTemplate = false`, `donate.stripe.com`, `Buy Dev a Coffee`, or `width: 320` remain
- `updateStatusIcon` uses SF Symbol with `isTemplate = true`
- Popover contentSize width is 360, matching SwiftUI frame
</verification>

<success_criteria>
1. Menu bar icon renders as monochrome sparkle that adapts to light/dark mode
2. No donation/coffee button visible in the popover
3. Popover content is not horizontally clipped
4. Icon renders crisply on Retina displays (SF Symbol is vector-based)
5. App builds and runs without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-icon-and-cleanup/01-01-SUMMARY.md`
</output>
